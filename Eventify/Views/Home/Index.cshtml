@{
    ViewData["Title"] = "Home Page";
}

<!-- Hero Section -->
<section class="position-relative py-5" style="min-height: 100vh; display: flex; align-items: center; justify-content: center; background: linear-gradient(rgba(0,0,0,0.4), rgba(0,0,0,0.4)), url('/images/hero-bg.jpg') center/cover;">
    <div class="container-lg d-flex flex-column justify-content-center align-items-center text-center">
        <div class="row w-100">
            <div class="col-lg-8 mx-auto">

                <h1 class="display-3 fw-bold text-white mb-4">Find the Perfect Venue and Event for You</h1>
                <p class="lead text-white-50 mb-5">Discover amazing venues and unforgettable events in your area</p>

                <!-- Search Bar -->
                @* <div class="bg-white rounded-3 p-4 mb-4 shadow-lg"> *@
                @*     <div class="row g-3"> *@
                @*         <div class="col-md-3"> *@
                @*             <div class="input-group"> *@
                @*                 <span class="input-group-text bg-light border-0"><i class="fas fa-map-marker-alt"></i></span> *@
                @*                 <input type="text" class="form-control border-0" placeholder="Location"> *@
                @*             </div> *@
                @*         </div> *@
                @*         <div class="col-md-3"> *@
                @*             <div class="input-group"> *@
                @*                 <span class="input-group-text bg-light border-0"><i class="fas fa-calendar"></i></span> *@
                @*                 <input type="date" class="form-control border-0"> *@
                @*             </div> *@
                @*         </div> *@
                @*         <div class="col-md-3"> *@
                @*             <div class="input-group"> *@
                @*                 <span class="input-group-text bg-light border-0"><i class="fas fa-users"></i></span> *@
                @*                 <select class="form-select border-0"> *@
                @*                     <option>Event Type</option> *@
                @*                     <option>Wedding</option> *@
                @*                     <option>Corporate</option> *@
                @*                     <option>Birthday</option> *@
                @*                     <option>Conference</option> *@
                @*                 </select> *@
                @*             </div> *@
                @*         </div> *@
                @*         <div class="col-md-3"> *@
                @*             <button class="btn btn-primary w-100"><i class="fas fa-search me-2"></i>Search</button> *@
                @*         </div> *@
                @*     </div> *@
                @* </div> *@

                <!-- CTA Buttons -->
                <div class="d-flex flex-wrap gap-3 justify-content-center">
                    <a href="@Url.Action("Index", "Venues")" class="btn btn-primary btn-lg px-4 glow-button">Browse Venues</a>
                    <a href="@Url.Action("Index", "Events")" class="btn btn-primary btn-lg px-4">Explore Events</a>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Featured Venues Section -->
<section class="py-5 bg-light">
    <div class="container-lg">
        <div class="text-center mb-5">
            <h2 class="display-5 fw-bold mb-3">Popular Venues</h2>
            <p class="lead text-muted">Discover our handpicked selection of premium venues perfect for any occasion</p>
        </div>

        <div class="row g-4">
            <!-- Venue Card 1 -->
            @await Html.PartialAsync("_VenueCard")

            <!-- Venue Card 2 -->
            @await Html.PartialAsync("_VenueCard")

            <!-- Venue Card 3 -->
            @await Html.PartialAsync("_VenueCard")
        </div>

        <div class="text-center mt-5">
            <a href="@Url.Action("Index", "Venues")" class="btn btn-outline-primary btn-lg">View All Venues</a>
        </div>
    </div>
</section>

<!-- Featured Events Section -->
<section class="py-5">
    <div class="container-lg">
        <div class="text-center mb-5">
            <h2 class="display-5 fw-bold mb-3">Upcoming Events</h2>
            <p class="lead text-muted">Don't miss out on these exciting events happening near you</p>
        </div>

        <div class="row g-4">
            <!-- Event Card 1 -->
            @await Html.PartialAsync("_EventCard")

            <!-- Event Card 2 -->
            @await Html.PartialAsync("_EventCard")
        </div>

        <div class="text-center mt-5">
            <a href="@Url.Action("Index", "Events")" class="btn btn-outline-primary btn-lg">View All Events</a>
        </div>
    </div>
</section>